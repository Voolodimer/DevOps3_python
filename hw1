1. Составить таблицу соответствия между различными объектами основных классов: int, str и объектами класса bool.

В контексте логических операций, а также когда выражения используются
 операторами потока управления, следующие значения интерпретируются
 как false: False, None, числовой ноль всех типов, а также пустые
 строки и контейнеры (включая строки, кортежи, списки, словари,
 наборы и замороженные наборы).
 Все остальные значения интерпретируются как истинные.
---------------------------------------
    |              bool               |
---------------------------------------
int |   0 == bool(0) == False         |
    |   1 == bool(1) == True          |
str |   '', "" == bool('') == False   |
    |   ' ', 'x' == bool('x') == True |
---------------------------------------
2. Разобраться с различиями между input() и raw_input(). Также в контексте разных версий python: 2 и 3.
PEP 3111 Starting with Python 3, raw_input() was renamed to input().
В python2 raw_input записывал введённое значение как строку, например при вводе числа 5 записывалась строка "5". 
input() же, в свою очередь, старался изначально подбирать верный тип, при вводе 5.0 выбирался тип float, но в python3 input() работает как raw_input в python2

3. Найти и прочитать PEP про изменение print между python2 и python3.
- PEP 3105 Опысывает, то что Print превратили в функцию
- При делении целых цисел результат вецественное число (Python3)
- По умолчанию строки хранятся в unicod
- Введена ф-я range() вместо xrange()
- Версия python3 обратно не совместима

